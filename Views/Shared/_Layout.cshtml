@using Microsoft.AspNetCore.Identity
@using Do_an_co_so.Models
@inject UserManager<AppUser> UserManager
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Hệ thống tìm trọ thông minh</title>

    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/Do_an_co_so.styles.css" asp-append-version="true" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

    <style>
        body {
            background-color: var(--bg-color);
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        .navbar-custom {
            background-color: #ffffff;
            border-bottom: 1px solid #eaeaea;
            box-shadow: 0 4px 15px rgba(0,0,0,0.05);
            padding: 0.8rem 0;
        }

            .navbar-custom .navbar-brand {
                font-size: 1.5rem;
                color: #0d6efd;
                font-weight: 700;
            }

        .footer-main {
            background-color: #1a1d20;
            color: #a0aab2;
            padding: 4rem 0 2rem;
            margin-top: auto;
        }

        .footer-link {
            color: #a0aab2;
            text-decoration: none;
            display: block;
            margin-bottom: 0.8rem;
            transition: 0.2s;
        }

            .footer-link:hover {
                color: #0d6efd;
                padding-left: 5px;
            }
    </style>
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-custom sticky-top">
            <div class="container">
                <a class="navbar-brand" asp-controller="Home" asp-action="Index">
                    <i class="bi bi-house-heart-fill me-1"></i> RoomFinder
                </a>

                <button class="navbar-toggler border-0 shadow-none" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarContent">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0 align-items-lg-center">
                        <li class="nav-item"><a class="nav-link" asp-controller="Home" asp-action="Index">Trang chủ</a></li>
                        <li class="nav-item"><a class="nav-link" asp-controller="PhongTro" asp-action="TimKiem">🔍 Tìm trọ</a></li>

                        @if (User.Identity.IsAuthenticated)
                        {
                            <li class="nav-item"><a class="nav-link" asp-controller="Chat" asp-action="Index">💬 Tin nhắn</a></li>
                            <li class="nav-item"><a class="nav-link text-warning" asp-controller="Chat" asp-action="GlobalChat">🌍 Chat chung</a></li>

                            @if (User.IsInRole("ChuTro"))
                            {
                                var currentUser = await UserManager.GetUserAsync(User);
                                <li class="nav-item ms-lg-2 d-flex align-items-center">
                                    <span class="badge bg-success-subtle text-success border border-success-subtle rounded-pill px-3 py-2">
                                        Ví: @(currentUser?.SoDu.ToString("N0") ?? "0")đ
                                    </span>
                                </li>
                                <li class="nav-item ms-2 d-flex align-items-center">
                                    <a class="btn btn-sm btn-outline-success rounded-pill fw-bold px-3" asp-controller="PhongTro" asp-action="NapTien">
                                        <i class="bi bi-credit-card me-1"></i> Nạp tiền
                                    </a>
                                </li>
                                <li class="nav-item ms-2"><a class="nav-link text-primary fw-bold" asp-controller="PhongTro" asp-action="Create">+ Đăng phòng</a></li>
                                <li class="nav-item"><a class="nav-link" asp-controller="PhongTro" asp-action="QuanLyPhong">Quản lý tin</a></li>
                            }
                        }
                    </ul>

                    <div class="d-flex align-items-center">
                        <partial name="_LoginPartial" />
                    </div>
                </div>
            </div>
        </nav>
    </header>

    <div class="container mt-4 flex-grow-1">
        <main role="main">
            @RenderBody()
        </main>
    </div>

    <footer class="footer-main">
        <div class="container">
            <div class="row g-4 text-start">
                <div class="col-lg-4">
                    <h5 class="text-white"><i class="bi bi-house-heart-fill text-primary me-2"></i> RoomFinder</h5>
                    <p>Giải pháp tìm kiếm phòng trọ hàng đầu dành cho sinh viên.</p>
                </div>
                <div class="col-lg-2">
                    <h5 class="text-white">Menu</h5>
                    <a href="/" class="footer-link">Trang chủ</a>
                    <a href="/PhongTro/TimKiem" class="footer-link">Tìm kiếm</a>
                    <a href="/Home/AboutUs" class="footer-link">Về chúng tôi</a>
                </div>
                <div class="col-lg-3">
                    <h5 class="text-white">Hỗ trợ</h5>
                    <a href="/Home/Terms" class="footer-link">Chính sách bảo mật</a>
                    <a href="/Home/AboutUs" class="footer-link">Hướng dẫn đăng tin</a>
                </div>
                <div class="col-lg-3">
                    <h5 class="text-white">Liên hệ</h5>
                    <p class="small mb-1"><i class="bi bi-geo-alt text-primary me-2"></i> Quận Bình Thạnh, TP.HCM</p>
                    <p class="small"><i class="bi bi-envelope text-primary me-2"></i> hotro@roomfinder.com</p>
                </div>
            </div>
            <hr class="mt-4 border-secondary">
            <div class="text-center small pb-2">
                &copy; 2026 - Nền tảng hỗ trợ tìm trọ sinh viên
            </div>
        </div>
    </footer>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>